/* eslint-disable no-unused-vars */
import React from "react";
import PropTypes from "prop-types";

const BigFooter = (props) => {
  return <div>BigFooter</div>;
};

BigFooter.propTypes = {};

export default BigFooter;
